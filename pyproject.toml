[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["astmio"]

[project]
name = "astmio"
version = "1.0.0a3"
authors = [
  { name="Prasoon Pratham", email="prathamprasoonyt@gmail.com" },
]
description = "Python implementation of ASTM E1381/1394 protocol."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pydantic>=2.0.0",
    "structlog>=23.1.0",
    "PyYAML>=6.0",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
"Homepage" = "https://github.com/PrasoonPratham/astmio"
"Bug Tracker" = "https://github.com/PrasoonPratham/astmio/issues"

[project.optional-dependencies]
dev = ["ruff", "pytest", "pytest-asyncio", "hypothesis", "black", "pre-commit"]
hipaa = ["dataset>=1.6.2"]
metrics = ["prometheus-client>=0.17.0"]
prometheus = ["prometheus-client>=0.17.0"]
security = ["cryptography>=41.0.0"]
full = ["dataset>=1.6.2", "prometheus-client>=0.17.0", "cryptography>=41.0.0"]
docs = ["furo", "sphinx", "sphinx-autobuild"]

[tool.black]
line-length = 80
target-version = ['py38']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''

[tool.ruff]
line-length = 80
indent-width = 4
target-version = "py38"

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
    "B904",  # exception chaining (can fix later)
    "B017",  # blind exception catching in tests
    "C901",  # too complex
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.ruff.per-file-ignores]
"codec.py" = ["F401"]
